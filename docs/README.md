# Docs Folder Notes

- `docs.json` must always be valid JSON. **Note:** JSON does not support comments.
- Treat `docs.json` as human-managed content; avoid making automated edits.
- To propose database changes, use `changes.sql` rather than editing `docs.json` directly.
